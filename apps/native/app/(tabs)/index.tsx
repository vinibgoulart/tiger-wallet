import { Layout } from '../../components/Layout';
import { Text } from 'react-native';

const TabOneScreen = () => {
  return (
    <Layout>
      <Text>Tab One</Text>
    </Layout>
  );
};

export default TabOneScreen;
